@model IEnumerable<Capstone.Models.OrderModel>

@{
    ViewBag.Title = "Index";
}

<h2>Order History</h2>


<table class="table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.orderId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumBadge)
        </th>
        <th>
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.orderId)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.NumBadge)
        </td>
        

          @if (new string[] { "Bogey@email.com", "applejuice@gmail.com", "user3" }.Contains(this.User.Identity.Name))
          {
              
          }
          else
          {
              <td>
                  @Html.ActionLink("Clone Order", "Clone", new { id = item.orderId }) |
                  @Html.ActionLink("Delete Order", "Delete", new { id = item.orderId })
              </td>
           
          }



    </tr>
}

</table>
